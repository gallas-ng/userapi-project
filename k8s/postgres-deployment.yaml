apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: userapi
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
      - name: postgres
        image: postgres:15
        env:
        - name: POSTGRES_DB
          value: userdb
        - name: POSTGRES_USER
          value: vagrant
        - name: POSTGRES_PASSWORD
          value: password

        ports:
        - containerPort: 5432
